apiVersion: v1
kind: Service
metadata:
  name: fair-credit-scorer-bias-mitigation-service
  namespace: production
  labels:
    app: fair-credit-scorer-bias-mitigation
spec:
  selector:
    app: fair-credit-scorer-bias-mitigation
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP